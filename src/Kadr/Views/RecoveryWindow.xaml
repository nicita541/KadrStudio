<Window x:Class="KadrStudio.Views.RecoveryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Восстановление — Kadr Studio"
        Width="680" Height="460" MinWidth="560" MinHeight="360"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="{StaticResource WindowBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Восстановление проекта" FontSize="22" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Margin="0,7,0,16"
                   Text="Выберите нужное состояние. Kadr Studio хранит до 20 проверенных версий каждого несохранённого проекта."
                   Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="9" Padding="8">
            <ListBox x:Name="RecoveryList" ItemsSource="{Binding Recoveries}" SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Reason}" Margin="0,5,0,0"
                                           Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="18,0,0,0" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding UpdatedAt, StringFormat={}{0:dd.MM.yyyy HH:mm:ss}}" />
                                <TextBlock Text="{Binding Revision, StringFormat=Ревизия {0}}"
                                           Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Right" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource ButtonBase}" Content="Продолжить без восстановления"
                    Click="Continue_Click" Padding="14,8" Margin="0,0,8,0" />
            <Button Style="{StaticResource AccentButton}" Content="Восстановить выбранное"
                    Click="Restore_Click" Padding="16,8" />
        </StackPanel>
    </Grid>
</Window>
